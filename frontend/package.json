{
  "name": "roomiefit-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Static server + /api proxy for the RoomieFit web frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
