{
  "name": "roomiefit-campusnest-israel",
  "version": "0.1.0",
  "private": true,
  "description": "RoomieFit / CampusNest Israel public web deployment server.",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "npm run build && nodemon backend/server.js",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
